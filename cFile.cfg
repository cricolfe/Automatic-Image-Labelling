// Config values are for the images of hands.


// base path where all files and folders are
basePath = "images/";

// background image to subtract fromthe current one
backgroundFile = "imbk.jpg";

// Path to images of backgrounds to create new images
backgroundPath = "backgrounds";

// Threshold to select pixels
threshold = 15;

// HSV channel -> (0 - Hue)  (1 - Saturation)  (2 - Value)
channel = 0;

// Maximum and Minimum number of points per countours
numberMaxPointsThreshold = 35;
numberMinPointsThreshold = 8;

// Minimum number of points per countours
numberPointsMaxThreshold = 10;

// Folder to store resulting images
resultFolder = "results";

// region atributes name
regionAtributes = "fingers";

// Base name for resulting images
resultImageFileName = "img";

// Wait time between images to show results (ms)
waitTime = 0;

// Folders with classes images. Objects in folder images are labeled with its folders name
// classes: {"one"; "two"; "three"; "four"; "five";};
// IDs = ["four"];
IDs = ["one", "two", "three", "four", "five"];

// Number for the classes in the final data files
// IDsNumber = [4];
IDsNumber = [1, 2, 3, 4, 5];

// Maximun distance aproximation of the poligon to the real countour
aproximation = 6;

// File name to save results
dataFile = "allData.csv";
